server:
  port: 8003

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 30000

zuul:
  ignoredServices: '*' #忽略服务  *代表所有的服务都会被忽略
  host:
    connect-timeout-millis: 30000  #连接超时（豪秒）
    socket-timeout-millis: 30000
  routes:
    elk-platform-auth:
      path: /auth/**
    elk-service-upms: /upms/**
    elk-service-basis: /basis/**
    elk-service-visit: /visit/**
    elk-service-quartz: /quartz/**
    elk-service-vehicle: /vehicle/**
    elk-service-student: /stu/**
    elk-service-bespeak: /bespeak/**
    elk-service-oss: /oss/**
    elk-service-coach: /app/coach/**
    elk-service-salesman: /app/salesman/**
    elk-service-app-student: /app/student/**
    elk-service-activiti: /activiti/**
    elk-service-visualization:  /visualization/**
  ratelimit:
    key-prefix: elk-ratelimite #对应用来标识请求的key的前缀
    enabled: true
    repository: REDIS #对应存储类型（用来存储统计信息）
    behind-proxy: true #代理之后
    policies:
      elk-upms-service: #特定的路由
        limit: 100 #可选 - 每个刷新时间窗口对应的请求数量限制
        quota: 100 #可选-  每个刷新时间窗口对应的请求时间限制（秒）
        refresh-interval: 3 # 刷新时间窗口的时间，默认值 (秒

security:
  validate:
    code: false
  sessions: stateless
  oauth2:
    client:
      client-id: ENC(DRVvRUB7c4AVFEFuyjBiUw==)
      client-secret: ENC(CxerGjQOXqJ3c+egeJrtIQ==)
    resource:
      loadBalanced: true
      token-info-uri: ${security.auth.server}/oauth/check-token
      service-id: elk-platform-zuul
      jwt:
         key-uri: ${security.auth.server}/oauth/token_key #解析jwt令牌所需要密钥的地址

urls:
  anon:
    - /auth/**
    - /*/code/*
    - /swagger-resources/**
    - /swagger-ui.html
    - /swagger-ui.html/**
    - /*/v2/api-docs
    - /swagger/api-docs
    - /upms/user/info
    - /webjars/**
    - /basis/**
    - /activiti/**
    - /activiti/404.html
    - /excle/**
    - /app/**
    - /oss/**
    - /visualization/**
    - /stu/upms/**
